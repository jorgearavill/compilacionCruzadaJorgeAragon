# compilacionCruzadaJorgeAragon

make & make install must be run in order for this utility to work

To execute the program, a symlink must be created. for sleep: ln -s minibusybox lsmod_

to execute: ./lsmod_
